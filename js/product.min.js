async function main(){displayCamerasDetails()}function getCamera(id){return fetch(`http://localhost:3000/api/cameras/${id}`).then((function(httpbodyResponse){if(404===httpbodyResponse.status)throw new Error;return httpbodyResponse.json()})).then((function(camera){return camera}))}async function displayCamerasDetails(){const urlParams=window.location.search,searchParams=new URLSearchParams(urlParams),productId=searchParams.get("id"),cameraDetail=await getCamera(productId);document.getElementById("cameraProduct").innerHTML+=`\n        <div class="cameraProductBox">\n            <img src=${cameraDetail.imageUrl}>\n        <div/>\n        <div class="cameraDescriptionBox">\n        <h2 class="camera">${cameraDetail.name}</h2>\n        <p>${cameraDetail.description}</p>\n        <p>${cameraDetail.price/100+" €"}</p>      \n        <button class="buttonPanier" onclick="location.href='panier.html'" type="button"> Ajouter au panier </button>\n        </div>        \n        </select>\n        </div>\n        </div>\n        `}main();var selector=document.createElement("select");document.body.appendChild(selector),cameraDetail.lenses.forEach(element=>{var option=document.createElement("option");document.getElementById("lenses").appendChild("option")});